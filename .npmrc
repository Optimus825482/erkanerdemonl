# Workspace hoisting bu projede sorun çıkarıyor (Next.js 16
# parent package.json'daki workspaces alanını buluyor ve kendi
# dizinimizden farklı yere yüklenen modülleri görmüyor).
# Bu projede npm'i tek başına çalıştır.
install-strategy=nested
